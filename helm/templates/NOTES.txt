Solo has been successfully installed.

Next steps:
- Acquire Grafana admin password:
`kubectl get secret -l app.kubernetes.io/name=grafana -o jsonpath="{.items[0].data.admin-password}" | base64 -d`

- Acquire Minio credentials:
`kubectl get secret -l app=minio -o jsonpath="{.items[0].data.rootUser}" | base64 -d`
`kubectl get secret -l app=minio -o jsonpath="{.items[0].data.rootPassword}" | base64 -d`

