Solo has been successfully installed.

Next steps:
- Acquire Grafana admin password:
`kubectl get secret -l app.kubernetes.io/name=grafana -o jsonpath="{.items[0].data.admin-password}" | base64 --decode`


